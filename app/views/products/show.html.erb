<h1 class="productName"><%= @product.name %></h1>
<a href="#" class="js-next" data-id="<%= @product.id %>">Next Product</a>
<h4 class="productInv">Inventory: <%= @product.inventory %></h4>
<h4 class="productPrice">Price: <%= @product.price %></h4>
<p class="productBody"><%= @product.description %></p>

<script type="text/javascript">
$(function () {
  $(".js-next").on("click", function() {
      var nextId = parseInt($(".js-next").attr("data-id")) + 1;
        $.getJSON('/products/' + nextId + '/data', function(data){
          $(".productName").text(data["name"])
          $(".productInv").text(data["inventory"])
          $(".productPrice").text(data["price"])
          $(".productBody").text(data["description"])
      })
    })
  })
</script>
